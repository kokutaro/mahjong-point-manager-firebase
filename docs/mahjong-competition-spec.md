# 麻雀大会機能 仕様書

## 1. 概要

### 1.1 目的

麻雀大会における各卓の結果と参加者の得点を一元管理し、集計を容易にする。
主催者は大会を作成してルールを統一し、複数卓の進行状況をリアルタイムに把握しながら運営できる。

### 1.2 スコープ

本仕様は、既存の部屋（Room）ベースの対局管理機能をベースに、大会という上位概念を導入する。
既存の点数計算・ゲーム進行ロジック（`gameLogic.ts`, `scoreCalculator.ts` 等）はそのまま再利用し、大会固有のデータモデル・画面・ワークフローを追加する。

---

## 2. 用語定義

| 用語                                  | 説明                                                                                     |
| ------------------------------------- | ---------------------------------------------------------------------------------------- |
| **大会 (Competition)**                | 1つのイベント単位。複数の卓と参加者を持つ。                                              |
| **大会シリーズ (Competition Series)** | 一定期間の複数大会を開催回として束ね、参加者と成績を統合する大枠。                       |
| **シリーズ参加者 (Series Member)**    | 開催回ごとのID変更に影響されない、シリーズ内の安定した参加者単位。                       |
| **開催回 (Series Round)**             | シリーズに第N回として紐付く既存の大会。                                                  |
| **主催者 (Organizer)**                | 大会を作成し、卓の管理・参加者のアサインなどを行うユーザー。プレイヤーとしても参加可能。 |
| **共同主催者 (Co-Organizer)**         | 主催者から管理権限を付与されたユーザー。主催者と同等の卓管理・アサイン操作が可能。       |
| **参加者 (Participant)**              | 大会に参加登録したプレイヤー。アカウント登録済みユーザーまたはゲスト（名前のみ）。       |
| **卓 (Table)**                        | 大会内の1つの対局場。3〜4人のプレイヤーがアサインされ、対局を行う。                      |
| **対局 (Game)**                       | 1回の半荘または東風戦。既存の `GameResult` に対応する。                                  |
| **アサイン (Assign)**                 | 参加者を卓に配置すること。                                                               |
| **デアサイン (Unassign)**             | 参加者を卓から外すこと。                                                                 |

---

## 3. 大会のライフサイクル

### 3.1 ステータス遷移

```text
[recruiting] → [in_progress] → [closed] → [archived]
   募集中         進行中          集計完了       アーカイブ
```

| ステータス    | 説明                                           | 許可される操作                                                   |
| ------------- | ---------------------------------------------- | ---------------------------------------------------------------- |
| `recruiting`  | 参加者募集中。卓は作成可能だが対局は開始不可。 | 参加登録、卓作成、ルール変更、大会情報編集                       |
| `in_progress` | 大会進行中。参加登録も引き続き可能。           | 参加登録、卓作成/削除、アサイン/デアサイン、対局の開始/進行/終了 |
| `closed`      | 主催者が大会を終了。集計レポートが確定する。   | レポート閲覧、CSV/PDFエクスポート                                |
| `archived`    | 読み取り専用のアーカイブ状態。                 | レポート閲覧のみ                                                 |

### 3.2 ステータス遷移条件

- **recruiting → in_progress**: 主催者が「大会開始」を実行。
- **in_progress → closed**: 主催者が「大会終了」を実行。進行中の卓がある場合は警告を表示し、確認の上で終了可能。
- **closed → archived**: 自動遷移（一定期間後）または主催者の操作。

---

## 4. 大会作成・設定

### 4.1 大会作成

主催者がログイン状態で以下の情報を入力し、大会を作成する。

| 項目       | 必須 | 説明                                                                 |
| ---------- | ---- | -------------------------------------------------------------------- |
| 大会名     | ○    | 任意の名前（最大50文字）                                             |
| 説明       | ×    | 大会の説明文（最大500文字）                                          |
| パスコード | ×    | 大会参加時に必要なパスコード。設定しない場合はリンクのみで参加可能。 |

### 4.2 共有機能

- 大会作成後、参加用の **URL** が発行される。
- URLは **QRコード** としても表示可能（画面上でのQR生成）。
- URLにアクセスした際、パスコードが設定されている場合は入力を求める。

### 4.3 共同主催者

- 主催者は、大会が `recruiting` または `in_progress` の間、Firebase Auth UID と紐づく参加ユーザーを **共同主催者** に任命・解除できる。匿名認証ユーザーも対象に含むが、UID を持たない代理登録ゲストは対象外とする。
- 共同主催者は以下の操作が可能:
  - 卓の作成/削除
  - プレイヤーのアサイン/デアサイン
  - 対局の管理
- 以下の操作は **主催者のみ**:
  - 大会ルールの変更
  - 大会の開始/終了
  - 共同主催者の任命/解除
  - 大会情報（名前・パスコード等）の編集

---

## 5. 大会ルール設定

### 5.1 概要

大会ルールは既存の `GameSettings` をベースとし、大会内のすべての卓に統一的に適用される。
卓ごとに変更可能なのは **3麻/4麻の切り替え（`mode`）のみ** とする。

### 5.2 大会ルール設定項目

既存の `GameSettings` の全項目が設定可能。主要項目を再掲:

| 項目               | 説明            | デフォルト |
| ------------------ | --------------- | ---------- |
| `length`           | 半荘 / 東風     | 半荘       |
| `startPoint4ma`    | 配給原点（4麻） | 25000      |
| `startPoint3ma`    | 配給原点（3麻） | 35000      |
| `returnPoint4ma`   | 返し点（4麻）   | 30000      |
| `returnPoint3ma`   | 返し点（3麻）   | 40000      |
| `uma`              | ウマ            | [10, 30]   |
| `hasHonba`         | 本場の有無      | あり       |
| `honbaPoints`      | 本場点          | 300        |
| `tenpaiRenchan`    | テンパイ連荘    | あり       |
| `useTobi`          | トビの有無      | あり       |
| `useChip`          | チップの有無    | なし       |
| `chipRate`         | チップレート    | -          |
| `useOka`           | オカの有無      | あり       |
| `useFuCalculation` | 符計算の有無    | あり       |
| `noFuFixedPoints`  | 符なし固定点    | (既定値)   |
| `westExtension`    | 西入の有無      | なし       |
| `rate`             | レート          | 0          |

### 5.3 卓固有の設定

卓作成時に、以下の項目のみ卓ごとに設定可能:

| 項目   | 説明                                                 |
| ------ | ---------------------------------------------------- |
| `mode` | `'3ma'` / `'4ma'` — 大会内で3麻卓と4麻卓の混在を許可 |

上記以外のルール設定は大会ルールから継承され、卓ごとに変更不可。

卓の対局開始時、`mode` に応じて `startPoint4ma` / `startPoint3ma` が `GameSettings.startPoint` として、`returnPoint4ma` / `returnPoint3ma` が `GameSettings.returnPoint` としてそれぞれ適用される。

---

## 6. 参加者管理

### 6.1 参加方法

大会への参加方法は以下の2通り:

#### A. アカウント登録済みユーザー

- 大会URLにアクセスし、ログイン状態で「参加」を実行。
- Firebase Auth の UID と紐づけて管理される。
- ダッシュボードの戦績に大会結果が反映される。

#### B. ゲスト参加（匿名）

- アカウント未登録でも、名前を入力して参加可能。
- 主催者がその場で代理登録することも可能（参加者名を手動入力）。
- ゲスト参加者のデータは大会内でのみ有効。個人の戦績ダッシュボードには反映されない。
- 匿名認証で参加したゲストは Firebase Auth UID と紐づくため共同主催者に任命できる。主催者が名前だけで代理登録したゲストは任命できない。

### 6.2 参加者ステータス

| ステータス          | 説明                                 |
| ------------------- | ------------------------------------ |
| `idle` (空き)       | どの卓にもアサインされていない状態   |
| `assigned` (配置済) | 卓にアサインされている（対局開始前） |
| `playing` (対局中)  | アサインされた卓で対局進行中         |

### 6.3 参加者一覧

主催者・共同主催者は以下の情報を一覧で確認できる:

- 参加者名
- ステータス（空き / 配置済 / 対局中）
- 現在の所属卓（アサインされている場合）
- 累計ポイント
- 対局回数

---

## 7. 卓管理

### 7.1 卓の作成

- 主催者・共同主催者は任意のタイミングで卓を作成できる。
- 大会ステータスが `recruiting` または `in_progress` の間、卓の作成が可能。

| 項目     | 必須 | 説明                                                       |
| -------- | ---- | ---------------------------------------------------------- |
| 卓名     | ○    | 識別しやすい名前（例: "A卓", "1番テーブル"）。最大30文字。 |
| モード   | ○    | `'3ma'` / `'4ma'`                                          |
| 卓ランク | ○    | `1`（最上位）〜 `5`。既存の未設定卓はランク1として扱う。   |

### 7.2 卓のステータス

| ステータス            | 説明                                       |
| --------------------- | ------------------------------------------ |
| `open` (空き)         | プレイヤーのアサイン待ち。対局は未開始。   |
| `ready` (準備完了)    | 必要人数がアサインされ、対局開始可能。     |
| `playing` (対局中)    | 対局が進行中。                             |
| `finished` (対局完了) | 対局が完了し、次の対局待ちまたは解散待ち。 |

```text
[open] → [ready] → [playing] → [finished] → [open]
                                ↑             ↓
                                └── 連続対局 ─┘
```

### 7.3 卓の削除

- 卓のステータスが `open` の場合のみ削除可能。
- アサイン済みのプレイヤーがいる場合は、自動的にデアサインしてから削除する（確認ダイアログあり）。

### 7.4 3麻/4麻モードの変更

- 卓のステータスが `open` の場合のみ変更可能。
- アサイン済みのプレイヤーがいる場合、人数超過が発生する可能性があるため、変更前にデアサインを要求する。

### 7.5 プレイヤーのアサイン

- **条件**:
  - 卓のステータスが `open` または `ready` であること。
  - 対象プレイヤーのステータスが `idle`（空き）であること。
  - アサイン先の卓の定員（3麻: 3名、4麻: 4名）に空きがあること。
- 主催者・共同主催者がアサイン操作を実行する。
- 定員に達した卓は自動的に `ready` ステータスに遷移する。

### 7.6 プレイヤーのデアサイン

- **条件**:
  - 卓のステータスが `open` または `ready` であること（`playing` 中はデアサイン不可）。
- デアサインされたプレイヤーのステータスは `idle` に戻る。
- 定員を下回った卓は `open` ステータスに戻る。

### 7.7 自動アサイン

- 大会ダッシュボードの「自動アサイン」から、未アサインの参加者に対する割当案を作成できる。
- 対象は `open` または `ready` の卓の空席とし、対局中・終了済みの卓および既存の手動アサインは変更しない。
- 参加者は、当該大会での累計点数（降順）、平均順位（昇順）、参加日時、参加者IDの順に評価する。
- 対局実績がない参加者は累計点数を0、平均順位を未算出として扱う。同じ累計点数では平均順位が算出済みの参加者を先にする。
- 卓は、卓ランク（昇順）、作成日時、卓名、卓IDの順に評価し、上位参加者から上位卓の空席へ配置する。
- 自動アサインのボタン押下だけでは保存しない。確認画面には既存の手動配置と自動追加を区別して最終卓構成を表示し、運営者が「アサインする」を選択した場合のみ一括反映する。
- 確認中に成績、卓設定、卓構成、参加者状態が変わった場合は割当案を再生成し、再確認を要求する。
- 反映時は最新の卓・参加者状態をトランザクション内で検証し、競合を検出した場合は全件を反映せず再提案する。
- 反映後も従来の手動アサイン、デアサイン、席順変更を利用できる。

### 7.8 席順（風）の設定

アサインされたプレイヤーは、対局開始前に自分の席（東・南・西・北）を選択できる。

- **手動選択**: プレイヤーが空いている席を選択する。
- **ランダム**: 卓の全プレイヤーが確定した状態で「ランダム配席」ボタンにより自動決定。
- デフォルトはアサイン順に東→南→西→北が仮割り当てされる。

---

## 8. 卓内対局の進行

### 8.1 既存機能の再利用

卓内の対局進行は、既存の Room ベースの対局機能をそのまま利用する。
具体的には以下のロジック・UI を再利用:

- 点数計算 (`scoreCalculator.ts`)
- ゲーム進行管理 (`gameLogic.ts`)
- 点数入力UI (`ScoringModal`)
- スコアボード (`ScoreBoard`)
- 流局処理 (`RyukyokuBoard`)
- 結果表示 (`ResultView`)
- 精算表示 (`SettlementModal`)

### 8.2 対局の開始

- 卓のステータスが `ready` の状態で、アサインされたプレイヤーまたは主催者が「対局開始」を実行。
- 大会ルール（+ 卓固有のモード設定）が `GameSettings` として適用される。
- 卓のステータスは `playing` に遷移。
- アサインされたプレイヤーのステータスは `playing` に遷移。

### 8.3 対局中の操作権限

- **卓にアサインされたプレイヤー**: 点数入力・アガリ/流局の操作が可能（既存の同期モードと同様）。
- **主催者・共同主催者**: 同様にゲーム操作が可能（卓の監督として）。

### 8.4 対局の終了

- 既存のゲーム終了条件（トビ、オーラス終了等）により対局が終了する。
- 対局結果が `GameResult` として記録される。
- 卓のステータスは `finished` に遷移。

---

## 9. 卓内連続対局

### 9.1 概要

1つの卓・メンバーで複数回の対局を連続して行えるようにする。
対局終了後、同じ卓で次の対局を開始するか、卓を解散するかを選択できる。

### 9.2 対局終了後の選択肢

対局が完了した後、以下の選択肢を提示する:

1. **次の対局を開始**: 同じメンバー・同じ卓で新たな対局を開始する。
2. **卓を終了（解散）**: 卓を `open` ステータスに戻し、アサインされた全プレイヤーを `idle` に戻す。

### 9.3 連続対局の仕様

- 対局回数の上限は設けない（プレイヤーの判断に委ねる）。
- 各対局はそれぞれ独立した `GameResult` として記録される。
- 連続対局中の全結果は卓に紐づいて管理され、レポートで確認可能。
- 「次の対局を開始」を選択した場合:
  - 席順は前回のまま維持する（手動で変更も可能）。
  - 大会ルール（+ 卓のモード設定）が引き続き適用される。

### 9.4 卓の解散

- 「卓を終了」を選択した場合:
  - 卓のステータスが `open` に戻る。
  - 全プレイヤーのステータスが `idle` に戻る。
  - 主催者は再度別のプレイヤーをアサインして新たな対局を開始できる。

---

## 10. 各卓実況機能

### 10.1 概要

大会の進行状況を会場のプロジェクターやモニターに映すことを想定した、読み取り専用のライブビュー画面を提供する。

### 10.2 全卓一覧表示

- 全卓のスコア・局情報をタイル形式で一覧表示する。
- 各コンポーネントに表示する情報:
  - 卓名
  - ステータス（空き / 対局中 / 対局完了）
  - 各プレイヤーの名前・現在のスコア
  - 現在の局（例: "東2局 1本場"）
  - 当該卓の対局回数（何戦目か）

### 10.3 リアルタイム更新

- Firestore のリアルタイムリスナーを利用し、点数変動・局の遷移がリアルタイムに画面へ反映される。
- 操作は不要（表示専用画面）。

### 10.4 アクセス方法

- 大会の実況URL（`/competitions/{id}/live`）にアクセスすることで閲覧可能。
- ログイン不要（公開ビュー）。ただしパスコード設定のある大会は、パスコード入力が必要。

---

## 11. 集計・レポート機能

### 11.1 集計タイミング

- 大会ステータスが `in_progress` の間はリアルタイムに集計途中結果を確認可能。
- 大会を「終了（closed）」した時点で最終レポートが確定する。

### 11.2 順位決定方法

- 各対局終了時に算出されるウマ・オカ精算後のポイント（`PlayerGameResult.point`）を全対局分合計する。
- 合計ポイントの降順で大会順位を決定する。
- 対局回数の差異は考慮しない（途中退出等で対局数が少ない参加者もそのまま合計ポイントで評価）。

### 11.3 レポート内容

#### 総合成績

| 列           | 説明                                   |
| ------------ | -------------------------------------- |
| 順位         | 大会内の総合順位                       |
| 参加者名     | プレイヤー名                           |
| 対局数       | 参加した対局の回数                     |
| 合計ポイント | 全対局のポイント合計                   |
| 平均順位     | 全対局での平均順位                     |
| チップ収支   | （チップ使用時のみ）全対局のチップ合計 |

#### 対局別詳細

| 列         | 説明                      |
| ---------- | ------------------------- |
| 卓名       | 対局が行われた卓          |
| 対局番号   | 卓内での対局回数          |
| 順位       | 当該対局での順位          |
| 素点       | 当該対局の素点            |
| ポイント   | ウマ/オカ精算後のポイント |
| チップ収支 | （チップ使用時のみ）      |

#### 卓別サマリ

| 列       | 説明                   |
| -------- | ---------------------- |
| 卓名     | 卓の識別名             |
| モード   | 3麻 / 4麻              |
| 対局回数 | その卓で行われた対局数 |
| 参加者   | 対局した全プレイヤー   |

### 11.4 エクスポート機能

- **CSV**: 総合成績と対局別詳細をCSV形式でダウンロード可能。
- **PDF**: レポート全体をPDF形式でダウンロード可能（印刷向けレイアウト）。

### 11.5 大会シリーズレポート

- シリーズダッシュボードの「レポート」から `/competition-series/{seriesId}/report` へ遷移する。
- 名寄せ済みの全開催回を横断し、シリーズ総合成績と参加者ごとの開催回別内訳を表示する。
- 総合成績には順位、参加者名、参加回数、対局数、合計ポイント、平均順位を表示する。チップを使用する開催回が1つ以上ある場合だけチップ収支を表示する。
- 進行中または募集中の開催回を含む場合は「途中結果」と集計日時を表示する。
- 未名寄せの成績参加者は対象の回・大会名・参加者名を警告し、取得できない開催回も警告する。不完全なデータがある間はPDF出力を無効にする。
- PDFは単大会レポートと同じブラウザ印刷機能を使用し、A4横向きの印刷スタイルで総合成績と開催回別内訳を出力する。サーバーへのPDF保存や集計値の永続化は行わない。

---

## 12. データモデル

### 12.1 Competition (大会)

Firestore の `competitions` コレクションに保存する。

```typescript
interface Competition {
  id: string; // 大会ID（自動生成）
  name: string; // 大会名
  description?: string; // 説明文
  organizerId: string; // 主催者のUID
  coOrganizerIds: string[]; // 共同主催者のUIDリスト
  status: CompetitionStatus; // 大会ステータス
  passcode?: string; // 参加パスコード（ハッシュ化して保存）
  hasPasscode: boolean; // パスコードの有無（クライアント表示用）
  settings: CompetitionSettings; // 大会ルール設定
  createdAt: number; // 作成日時（timestamp）
  startedAt?: number; // 大会開始日時
  closedAt?: number; // 大会終了日時
  seriesId?: string; // 親大会シリーズID（単独大会・既存データでは未設定）
  seriesRoundNumber?: number; // シリーズ内の1始まりの開催回番号
}

type CompetitionStatus = 'recruiting' | 'in_progress' | 'closed' | 'archived';

interface CompetitionSettings extends Omit<
  GameSettings,
  'mode' | 'isSingleMode' | 'startPoint' | 'returnPoint'
> {
  // mode は卓ごとに設定するため大会設定からは除外
  // isSingleMode は大会では使用しない
  // startPoint / returnPoint は 3麻/4麻 で個別に設定するため除外し、以下で再定義
  startPoint4ma: number; // 配給原点（4麻用）デフォルト 25000
  startPoint3ma: number; // 配給原点（3麻用）デフォルト 35000
  returnPoint4ma: number; // 返し点（4麻用）デフォルト 30000
  returnPoint3ma: number; // 返し点（3麻用）デフォルト 40000
}
```

### 12.2 CompetitionParticipant (参加者)

`competitions/{competitionId}/participants` サブコレクションに保存する。

```typescript
interface CompetitionParticipant {
  id: string; // 参加者ID（自動生成）
  userId?: string; // Firebase Auth UID（アカウント登録済みの場合）
  name: string; // 表示名
  isGuest: boolean; // ゲスト参加かどうか
  status: ParticipantStatus; // 参加者ステータス
  currentTableId?: string; // 現在アサインされている卓ID
  role: ParticipantRole; // 役割
  joinedAt: number; // 参加日時
  seriesMemberId?: string; // 大会シリーズ内の安定した参加者ID
}

type ParticipantStatus = 'idle' | 'assigned' | 'playing';

type ParticipantRole = 'organizer' | 'co_organizer' | 'player';
```

### 12.3 CompetitionTable (卓)

`competitions/{competitionId}/tables` サブコレクションに保存する。

```typescript
interface CompetitionTable {
  id: string; // 卓ID（自動生成）
  name: string; // 卓名
  rank?: 1 | 2 | 3 | 4 | 5; // 卓ランク（既存の未設定卓は1として扱う）
  mode: '3ma' | '4ma'; // 3麻 / 4麻
  status: TableStatus; // 卓ステータス
  playerIds: string[]; // アサインされた参加者IDリスト
  seatAssignment?: SeatAssignment; // 席順（風の割り当て）
  currentRoomId?: string; // 現在の対局に対応するRoom ID
  gameCount: number; // この卓で完了した対局数
  createdAt: number; // 卓作成日時
}

type TableStatus = 'open' | 'ready' | 'playing' | 'finished';

interface SeatAssignment {
  [participantId: string]: 'East' | 'South' | 'West' | 'North';
}
```

### 12.4 CompetitionGameResult (大会対局結果)

`competitions/{competitionId}/gameResults` サブコレクションに保存する。

```typescript
interface CompetitionGameResult {
  id: string; // 結果ID
  tableId: string; // 卓ID
  tableName: string; // 卓名（非正規化・レポート表示用）
  gameIndex: number; // 卓内の対局番号（1始まり）
  result: GameResult; // 既存の GameResult をそのまま格納
  participantIds: string[]; // 参加した参加者IDリスト（マッピング用）
  timestamp: number; // 対局完了日時
}
```

### 12.5 Room との連携

卓で対局を開始する際、既存の `rooms` コレクションに Room ドキュメントを作成する。  
Room には以下のフィールドを追加する:

```typescript
// RoomState への追加フィールド
interface RoomState {
  // ... 既存フィールド ...
  competitionId?: string; // 所属する大会ID（大会卓の場合のみ）
  tableId?: string; // 所属する卓ID（大会卓の場合のみ）
}
```

### 12.6 大会シリーズ

シリーズ本体を `competitionSeries/{seriesId}`、参加者を `members/{seriesMemberId}`、開催回を `rounds/{roundNumber}` に保存する。

```typescript
interface CompetitionSeries {
  id: string;
  name: string;
  description?: string;
  organizerId: string;
  coOrganizerIds: string[];
  startDate?: string; // YYYY-MM-DD
  endDate?: string; // YYYY-MM-DD
  createdAt: number;
  updatedAt: number;
}

interface CompetitionSeriesMember {
  id: string; // シリーズ内で不変のID
  userId?: string; // 任意。開催回ごとに異なる場合は名寄せで吸収する
  name: string;
  active: boolean;
  notes?: string;
  joinedAt: number;
}

interface CompetitionSeriesRound {
  id: string; // roundNumber の文字列表現
  competitionId: string;
  roundNumber: number; // 1始まり、シリーズ内で一意
  linkedAt: number;
}
```

- 新規開催回はシリーズ画面から通常の大会作成フォームを開き、作成完了後にシリーズへ紐付ける。
- 既存大会も大会IDと未使用の回番号を指定して手動で紐付けられる。
- 大会と開催回の相互参照はトランザクションで同時更新し、片側だけの状態を作らない。
- シリーズ参加者は任意の時点で追加できる。無効化しても過去成績は保持する。
- 参加リンクまたはQRコードから、認証済みユーザーが自分のAuth UIDをメンバーIDとして自己登録できる。本人による登録済みメンバーの更新は許可しない。
- 開催回へ参加する人だけをシリーズ参加者から選び、独立した大会参加者IDで追加する。
- 既存大会の参加者は `seriesMemberId` を手動選択して名寄せする。同一開催回で1人のシリーズ参加者へ複数人を重複して紐付けない。
- 既存大会の未名寄せ参加者は一括アサインできる。Auth UID一致、一意な正規化名一致の順で既存メンバーを使い、未一致または候補複数の場合は新規メンバーを作成する。メンバー作成と名寄せ更新は同一バッチで行う。

#### 12.6.1 総合成績

- 名寄せ済みの参加者のみを集計対象とする。未名寄せの成績参加者は警告一覧へ分離する。
- 合計ポイント、平均順位、チップ合計、対局数、成績が存在する参加回数、開催回別内訳を算出する。
- 総合順位は、合計ポイント降順、平均順位昇順、シリーズ参加日時昇順、シリーズ参加者ID昇順で決定する。
- 開催回ごとの参加人数や対局数の差を補正する足切り・ベストN回方式は本Issueの対象外とする。

#### 12.6.2 初回自動アサイン

- 現在の開催回に対局結果がない場合、前回までのシリーズ総合成績を卓ランク割当の評価値として選択できる。
- 過去成績は現在の大会を除外して集計し、当日の参加者の `seriesMemberId` へ適用する。
- 現在の開催回に結果が1件でも存在する場合は、従来どおり現在大会の累計ポイントと平均順位を使用する。
- 保存前の確認、手動配置の維持、状態変化時の再確認は従来の自動アサイン仕様を継承する。

---

## 13. Firestore セキュリティルール (概要)

### 13.1 competitions コレクション

- **読み取り**: 認証済みユーザーは一覧取得可能。パスコード付き大会の詳細は参加者のみ。
- **作成**: 認証済みユーザーのみ。
- **更新**: 主催者のみ（`organizerId == request.auth.uid`）。
- **削除**: 主催者のみ。ステータスが `recruiting` の場合のみ。

### 13.2 participants サブコレクション

- **読み取り**: 大会参加者・主催者・共同主催者。
- **作成**: 認証済みユーザー（自身の参加登録）。主催者によるゲスト代理登録。
- **更新**: 主催者・共同主催者のみ（ステータス変更・ロール変更）。
- **削除**: 主催者のみ。

### 13.3 tables サブコレクション

- **読み取り**: 大会参加者・主催者・共同主催者。実況URLからのアクセス（公開ビュー）。
- **作成/更新/削除**: 主催者・共同主催者のみ。

### 13.4 gameResults サブコレクション

- **読み取り**: 大会参加者・主催者。
- **作成**: 対局完了時にシステムが自動生成（Room の対局完了トリガー）。
- **更新/削除**: 不可。

### 13.5 competitionSeries コレクション

- **読み取り**: 認証済みユーザー。
- **シリーズ作成**: 認証済みユーザーが自分を主催者として作成する場合のみ。
- **シリーズ更新**: 主催者・共同主催者。`organizerId` は変更不可。
- **シリーズ削除**: 主催者のみ。
- **members 作成**: シリーズ主催者・共同主催者、または認証ユーザー本人。本人登録はドキュメントID・`id`・`userId` がAuth UIDと一致し、`active == true` の場合のみ許可する。
- **members 更新/削除**: シリーズ主催者・共同主催者のみ。ID、名前、有効状態を検証する。
- **rounds 作成**: シリーズ主催者・共同主催者のみ。対象大会側の `seriesId` と `seriesRoundNumber` が同一コミットで一致する場合のみ。作成済み回番号の上書きは不可。
- **参加者の名寄せ**: 大会主催者・共同主催者のみ。対象大会が属するシリーズに実在する `seriesMemberId` だけを許可する。

---

## 14. 画面構成

### 14.1 大会関連画面一覧

| 画面                   | パス (案)                               | 説明                                 |
| ---------------------- | --------------------------------------- | ------------------------------------ |
| 大会一覧               | `/competitions`                         | 自分が参加中 / 主催の大会一覧        |
| 大会作成               | `/competitions/new`                     | 大会の新規作成フォーム               |
| 大会参加               | `/competitions/{id}/join`               | 共有URLからの参加画面                |
| 大会ダッシュボード     | `/competitions/{id}`                    | 主催者・参加者向けの大会メイン画面   |
| 卓詳細                 | `/competitions/{id}/tables/{tableId}`   | 卓の対局画面（既存MatchPageベース）  |
| 実況ビュー             | `/competitions/{id}/live`               | プロジェクター投影用の全卓一覧       |
| レポート               | `/competitions/{id}/report`             | 集計レポート画面                     |
| 大会シリーズ一覧       | `/competition-series`                   | 大会シリーズの一覧                   |
| 大会シリーズ作成       | `/competition-series/new`               | シリーズの新規作成フォーム           |
| シリーズダッシュボード | `/competition-series/{seriesId}`        | 参加者、開催回、名寄せ、総合成績管理 |
| シリーズレポート       | `/competition-series/{seriesId}/report` | 総合成績、開催回別内訳、PDF印刷      |
| 大会シリーズ参加       | `/competition-series/{seriesId}/join`   | 共有リンク・QRからの本人参加登録     |

### 14.2 大会ダッシュボード

主催者・参加者が大会の全体状況を把握するための画面。

**主催者ビュー:**

- 大会情報（名前、共有URL/QR、パスコード）
- 参加者一覧（ステータス付き）
- 卓一覧（ステータス、アサイン状況、スコア概要）
- 卓の作成・削除・アサイン操作
- 大会の開始・終了ボタン

**参加者ビュー:**

- 大会情報（名前、ルール概要）
- 自分のステータス・所属卓
- 自分の累計ポイント・対局履歴
- 全卓の進行状況（実況ビューの簡易版）

### 14.3 大会シリーズダッシュボード

**共通表示:**

- シリーズ名、説明、開催期間
- QRコードと参加リンクの共有
- シリーズ総合順位と開催回別ポイント内訳
- シリーズ参加者一覧と有効状態
- 第N回の大会一覧、参加者、名寄せ状態
- 未名寄せ成績の警告
- シリーズレポートへの導線

**主催者・共同主催者操作:**

- シリーズ参加者の追加と有効・無効切り替え
- 次回大会の新規作成
- 既存大会の回番号付き紐付け・解除
- シリーズ参加者から当日の参加者を選択して開催回へ追加
- 開催回参加者とシリーズ参加者の手動名寄せ
- 既存大会参加者の一括アサイン

---

## 15. 非機能要件

### 15.1 パフォーマンス

- 実況ビューの更新遅延は 2秒以内を目標とする（Firestore リアルタイムリスナー依存）。
- 大会の参加者上限は 100名 を想定する。
- 同時稼働卓数は 25卓 を想定する。
- シリーズ総合成績は開催回のリアルタイム購読結果からクライアントで導出し、集計値を重複保存しない。

### 15.2 セキュリティ

- パスコードはクライアントに平文で保存しない。Firestore に保存する際はハッシュ化する。
- 共有URLにパスコード情報を含めない。
- 主催者権限の操作は Firestore セキュリティルールで厳密に制御する。

### 15.3 データ保持

- 大会データ（`archived` ステータス）は無期限で保持する。
- 将来的にストレージコスト最適化のため、古いデータの自動削除ポリシーを検討する。

---

## 16. 既存機能への影響

### 16.1 RoomState の拡張

- `competitionId` と `tableId` のオプショナルフィールドを追加。
- 既存の Room ベースの対局機能に影響なし（フィールドが未設定の場合は従来通り動作）。

### 16.2 UI の導線追加

- トップページに「大会」セクションまたはタブを追加。
- 既存の「部屋を作成」「部屋に参加」の導線はそのまま維持。

### 16.3 後方互換性

- 既存のデータ構造・ロジックへの変更は最小限とする。
- 大会機能は新規コレクション・新規画面として追加し、既存機能との競合を避ける。

---

## 17. 将来の拡張候補（スコープ外）

以下の機能は本仕様のスコープには含めないが、将来的な拡張候補として記録する:

- **スイスドロー方式**: 対戦履歴を考慮して同一組み合わせを避ける高度な自動組み合わせ。
- **大会内ラウンド管理**: 1つの大会内部を予選・決勝など複数ステージに分ける機能。複数大会を束ねる大会シリーズとは別概念とする。
- **チーム戦対応**: チーム単位での集計・順位決定。
- **オンライン大会対応**: 地理的に離れた参加者がリモートで参加する想定の拡張。
- **通知機能**: 卓のアサイン通知、対局開始通知など。
- **大会テンプレート**: よく使うルール設定のテンプレート保存・読み込み。
